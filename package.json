{
  "name": "bizfinder-ie",
  "private": true,
  "version": "0.0.0",
  "description": "Irish B2B business directory + analytics platform (working name)",
  "workspaces": [
    "packages/*",
    "api",
    "web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "api": "npm run dev --workspace api",
    "web": "npm run dev --workspace web",
    "build:shared": "npm run build --workspace @bizfinder/shared"
  }
}
